import React from 'react'

const Heading = ({text}) => {
    return (
        <div className="main-heading">
            <h1 className="main-heading__heading">{text}</h1>
        </div>
    )
}

export default Heading
